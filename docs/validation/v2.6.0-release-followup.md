# v2.6.0 发布前复查（2026-09-13）

## 结论

本轮确认的代码阻塞项已经修复，可进入最终发布验收。不能将自动测试通过等同于最终签名安装包和真实 TUN 网络已经验收。

## 修复范围

- `7eb3ac0` 已修复域名目标在兼容模式中绕过 IP 规则、父域名压过子域名例外、CI 掩盖测试失败，以及更新器测试的过期版本假设。
- 本次补充识别 Meta Tunnel、Mihomo、Clash 虚拟网卡，使它们遵循首页隐藏设置。这是显示分类，不是网卡使用禁令，也不会清除已有选择。
- 批量替换规则出口保留禁用状态与优先级；前端以确认时的现有规则状态为准，避免预览缺失字段重新启用规则。
- 保存后按匹配类型和值保持行标识，避免后端规范化优先级导致已有选中项失效。

## 验证

- 桌面端完整 `go test ./... -count=1 -timeout 120s`、`go vet ./...` 通过。
- 包含随附 sing-box 的真实出口选择、六种类型顺序、兼容模式、禁用和恢复子域名例外及热更新测试。测试仅使用本机 SOCKS 入站、确定性 DNS 和模拟出口，不启用 TUN 或修改系统代理。
- 前端 26 个测试文件、117 项测试通过；TypeScript 与生产前端构建通过。
- 新增页面交互回归：批量替换禁用规则后仍禁用；保存规范化后，选中规则仍可删除。
- Core 未在本轮修改；上一轮完整测试和静态检查通过，`7eb3ac0` 的 GitHub Core 与 Desktop CI 均成功。

## 发布前剩余验收

- 最终提交的 CI 构建完成，并验证最终签名安装包的覆盖升级、Core 服务启动、停止和卸载恢复。
- 真实 TUN/FakeIP、多网卡出口及第三方代理共存环境的分流验收；用连接记录确认实际出口。
- Steam 优选保持实验性、默认关闭；真实下载的暂停恢复、开关切换与长时间稳定性仍需实测。
- 本机未发现可用的 C 编译器，本轮未完成 Go race 检测；不能宣称已经证明并发路径不存在竞态。

## 未作为本轮缺陷处理的意见

- 旧配置缺少类型顺序时参考禁用规则的优先级，属于顺序恢复语义，不以“禁用即不应参考”为由直接改变；优先级 0 不代表禁用。
- Steam 实际响应 401/403 的失败归因和不同发现路径的缓存键仍是后续评估项，不能简单套用主动探测的语义或直接合并缓存键。

此结论仅覆盖已检查路径和上述证据，不是对所有运行环境和代码路径无缺陷的保证。
